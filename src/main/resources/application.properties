# 포트 설정(내장 톰캣이 사용할 포트 번호)
server.port=80

# view 경로 설정
spring.mvc.view.prefix = /WEB-INF/jsp/
spring.mvc.view.suffix = .jsp

#Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/final?characterEncoding=UTF-8&serverTimezone=UTC&SSL=false
spring.datasource.username=root
spring.datasource.password=tjoeun
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#mybatis pageHelper
pagehelper.helper-dialect=mysql
pagehelper.reasonable=true

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB

#spring json 기본 컨버터가 jackson 이므로 gson 으로 컨버터시 오류가 발생해서 기본을 gson으로 변경
spring.http.converters.preferred-json-mapper=gson